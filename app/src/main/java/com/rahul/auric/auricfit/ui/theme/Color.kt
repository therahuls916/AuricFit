package com.rahul.auric.auricfit.ui.theme

import androidx.compose.ui.graphics.Color

// Dark Theme Colors from the UI design
val BackgroundDark = Color(0xFF0A0A0A)       // Deep black background
val CardDark = Color(0xFF1C1C1E)            // Dark gray for cards
val PrimaryGreen = Color(0xFF34D7A1)        // The main accent green
val TextWhite = Color(0xFFFFFFFF)          // High-emphasis text
val TextGray = Color(0xFF8E8E93)            // Medium-emphasis text (subtitles, etc.)
val TextGrayMuted = Color(0xFF636366)      // Low-emphasis text (labels like 'kg')
val ToggleInactive = Color(0xFF767680)      // Inactive part of the toggle switch
val Separator = Color(0xFF38383A)          // Border/divider lines

// Light Theme (we define them for completeness, but will focus on dark)
val BackgroundLight = Color(0xFFF2F2F7)
val CardLight = Color(0xFFFFFFFF)
val PrimaryGreenLight = Color(0xFF00C781)
val TextBlack = Color(0xFF000000)
val TextGrayLight = Color(0xFF3C3C43)